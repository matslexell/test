name: Release preparation

on:
  workflow_dispatch:
  pull_request:
    branches:
      - staging
    types:
      - closed

jobs:
  create_draft_release:
    # if: github.event.pull_request.merged == true && startsWith(github.head_ref, 'release/v')
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Extract version
        id: get_version
        run: echo "version=v$(echo $(cat package.json) | jq -r .version)" >> $GITHUB_ENV

      - name: Create Tag
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN2 }}
        run: |
          git --global user.email "actions@github.com"
          git --global user.name "GitHub Actions"
          git tag ${{ env.version }}
          git push origin ${{ env.version }}

      - name: Create release
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN2 }}
        run: |
          gh release create ${{ env.version }} \
            --target staging \
            --title ${{ env.version }} \
            --draft \
            --generate-notes
